[build-system]
requires = ["scikit-build-core", "swig"]
build-backend = "scikit_build_core.build"

[project]
name = "bpm_backend"
version = "1.4.9"

[tool.scikit-build]
build.verbose = false
logging.level = "INFO"
build-dir = "bin"
cmake.args = ["-GNinja"]