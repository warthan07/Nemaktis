from setuptools import setup

setup(name = 'nemaktis',
      version = "1.0",
      description = 'Platform for light propgation in liquid crystal',
      author = 'Guilhem Poy',
      packages = ["nemaktis"]
)
