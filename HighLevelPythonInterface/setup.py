from setuptools import setup

setup(name = 'nemaktis',
      version = "1.3",
      description = 'Platform for light propagation in liquid crystal',
      author = 'Guilhem Poy',
      packages = ["nemaktis"]
)
